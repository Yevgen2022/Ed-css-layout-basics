# CSS Float Layout and Multi-Column Design

A page layout project focused on applying classic CSS techniques including float-based layout, column structures, and precise box-model control. The task was to match a mock-up layout while working only with the CSS file and keeping the HTML structure untouched.

## 🚀 Live Demo
👉 [Click here to view](https://your-live-demo-link.gitlab.io)

## 📁 Repository
👉 [GitLab Repo](https://gitlab.com/Yevhen2022/css-float-layout)

---

## 🧠 Key Concepts Applied

- Used `float` to position `<aside>` and `<main>` side-by-side according to classic layout rules
- Applied fixed `width` values to layout elements (`header`, `footer`, `main`, `aside`) to match the mock-up
- Used `position: fixed` and `top: 0` to make `.header_line` stay visible at the top during scrolling
- Controlled sizing using `box-sizing: border-box` to include paddings and borders in width/height calculations
- Implemented multi-column layout with `column-count` and `column-gap` for `.client_stories` section
- Applied `column-span: all` to section headings for full-width headings across multiple columns

---

## 🛠 Technologies

- HTML5
- CSS3
- CSS Float
- Box Model (`box-sizing`)
- Multi-column Layout (`column-count`, `column-gap`, `column-span`)
- GitLab Pages

---

## 📝 Task Instructions

See [REQUIREMENTS.md](./REQUIREMENTS.md) for the original training task description and technical requirements.
